If screen is not displaying properly:
1) Right click on the top of the console window
2) Select "Properties"
3) Select "Layout" Tab
4) Change "Window Size" to the following
   Width:  80
   Height: 30

Close the terminal and re-run the program. The graphics should be displaying properly